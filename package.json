{
  "name": "simple-network-check",
  "version": "1.0.0",
  "description": "Simple network check for availability and speed",
  "keywords": [
    "simple",
    "network",
    "check",
    "availability",
    "speed",
    "test",
    "connect",
    "connection",
    "disconnect",
    "disconnects",
    "response",
    "time",
    "timeout",
    "latency",
    "wifi"
  ],
  "author": "Uri Kalish (https://github.com/urikalish)",
  "contributors": [
    "Uri Kalish (https://github.com/urikalish)",
    "Yuval Koren (https://github.com/yuval-koren)"
  ],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/simple-network-check/simple-network-check.git"
  },
  "scripts": {
    "format": "prettier --write --config .prettierrc.json \"**/*.{html,css,ts}\"",
    "tsc": "tsc",
    "server": "live-server --port=8082 --host=localhost --open=public --watch=public",
    "dev": "yarn format && yarn tsc && yarn server"
  },
  "devDependencies": {
    "live-server": "^1.2.1",
    "prettier": "^2.1.2",
    "typescript": "^4.0.3"
  }
}

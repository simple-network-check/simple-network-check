{
  "name": "simple-network-check",
  "version": "1.0.0",
  "description": "Simple network check for availability and speed",
  "keywords": [
    "simple",
    "network",
    "check",
    "availability",
    "speed",
    "test",
    "connect",
    "connection",
    "disconnect",
    "disconnects",
    "response",
    "time",
    "timeout",
    "latency",
    "wifi"
  ],
  "author": "Uri Kalish (https://github.com/urikalish)",
  "contributors": [
    "Uri Kalish (https://github.com/urikalish)",
    "Yuval Koren (https://github.com/yuval-koren)"
  ],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/simple-network-check/simple-network-check.git"
  },
  "scripts": {
		"format": "prettier --write \"**/*.{html,css,ts}\"",
		"lint": "eslint --fix src/**/*.ts",
    "compile": "tsc",
		"build": "yarn format && yarn lint && yarn compile",
		"http-server": "http-server ./public --port 8042 --cors -c-1",
		"dev": "yarn build && yarn http-server"
  },
  "devDependencies": {
		"@typescript-eslint/eslint-plugin": "^5.29.0",
		"@typescript-eslint/parser": "^5.29.0",
		"concurrently": "^7.2.2",
		"deepmerge": "^4.2.2",
		"eslint": "^8.18.0",
		"http-server": "^14.1.1",
		"prettier": "^2.7.1",
		"rimraf": "^3.0.2",
		"tslib": "^2.4.0",
		"typescript": "^4.7.4"
  }
}
